apiVersion: v1
kind: Service
metadata:
  name: {{ include "fornex-webhook.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "fornex-webhook.name" . }}
    chart: {{ include "fornex-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "fornex-webhook.name" . }}
    release: {{ .Release.Name }}
